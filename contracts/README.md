# Contracts

This directory contains generated JSON Schemas and golden fixtures for the Agent Runtime and RAG Runtime HTTP APIs.

Regenerate schemas:

```bash
make contracts
```

Check schemas are current:

```bash
make contracts-check
```

